<div class="container">
      <div class="mt-5 col-md-5">
      <div class= "hover">
        <% @cameras.each do |camera| %>
          <div class="hover-img mb-3">
            <%= image_tag camera.image%>
          <div class="hover-text">
            <p class="text1"><%= camera.title %></p>
          </div>
          </div>
        <% end %>
      </div>
      </div>
    </div>
</div>